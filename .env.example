# Placeholder for a future real AI integration.
# Any real LLM call should go through your own backend route so the API
# key never ships to the browser — never call a provider directly from
# the React app in production.
VITE_AI_API_BASE_URL=https://your-backend.example.com/api
